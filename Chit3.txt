CREATE TABLE EMP(
EMP_ID INT,
EMP_NAME VARCHAR(10),
EMP_SAL INT,
DEPT_ID INT,
PRIMARY KEY(EMP_ID),
FOREIGN KEY (DEPT_ID) REFERENCES DEPT(DEPT_ID));

SELECT * FROM EMP;
DROP TABLE EMP;


CREATE TABLE DEPT(
DEPT_ID INT,
DEPT_NAME VARCHAR(10),
PRIMARY KEY(DEPT_ID));

SELECT * FROM DEPT;
DROP TABLE DEPT;

--Add Not Null constraint to empName
ALTER TABLE EMP 
MODIFY EMP_NAME VARCHAR(10) NOT NULL; 

--Insert few Record.
INSERT INTO EMP VALUES(1,'GANESH',50000,30);
INSERT INTO EMP VALUES(2,'VIHAN',20000,20);
INSERT INTO EMP VALUES(3,'AKASH',30000,10);
INSERT INTO EMP VALUES(1,'GANESH',50000,30);
INSERT INTO DEPT VALUES(10,'SOFTWARE');


--Add column deptloc varchar(10) to dept table
ALTER TABLE DEPT
ADD  DEPT_LOC VARCHAR(10);
 
--Add Unique to deptLoc.
ALTER TABLE DEPT
ADD UNIQUE (DEPT_LOC);

 